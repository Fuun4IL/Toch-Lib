{
  "name": "toch-lib",
  "version": "0.1.0",
  "description": "MVP: Angular utilities for SAP backends — OData v2/v4 query builder, adapter-based auth/SSO with Matomo logging, caching and CSRF interceptor.",
  "keywords": ["angular", "sap", "odata", "csrf", "sso", "cache", "matomo"],
  "peerDependencies": {
    "@angular/common": ">=16.0.0 <21.0.0",
    "@angular/core": ">=16.0.0 <21.0.0",
    "rxjs": "^7.4.0"
  },
  "dependencies": {
    "tslib": "^2.3.0"
  },
  "sideEffects": false
}
